public interface Exeggutor {
    //  public void executeDudeFullActivity(WorldModel world, ImageStore imageStore, EventScheduler scheduler)
    // in the Entity tab execute for the various values in the enum use this a lot
    void executeActivity(WorldModel world, ImageStore store, EventScheduler scheduler);
}

